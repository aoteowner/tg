name: tg
description: Telegram Client API (MTProto) to connect to Telegram and control a user programmatically.
version: 0.0.12
homepage: https://pwa.ir
repository: https://github.com/telegramflutter/tg
publish_to: none
topics:
  - telegram
  - tdlib

environment:
  sdk: ^3.8.0

# Add regular dependencies here.
dependencies:
  # t: ^0.0.26
  # t:
  #   git:
  #     url: https://github.com/aoteowner/t
  #     ref: 33671eb88
  tg_api:
    git:
      url: git@github.com:sersr/tg_api
      ref: master

  archive: ^4.0.7
  pointycastle:
  nop: ^0.2.0+3
  meta: any

dependency_overrides:
  tg_api:
    path: ../tg_api

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
